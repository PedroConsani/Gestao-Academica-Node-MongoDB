<h2>Minhas Matrículas</h2>

<a href="/aluno/matricula/nova" class="btn btn-primary">+ Nova Matrícula</a>

<table style="width: 100%; border-collapse: collapse; margin-top: 20px;">
    <thead>
        <tr style="background: #f1f3f5;">
            <th style="padding: 10px; text-align: left;">Curso</th>
            <th style="padding: 10px; text-align: left;">Ano Letivo</th>
            <th style="padding: 10px; text-align: left;">Estado</th>
            <th style="padding: 10px; text-align: center;">Data Solicitação</th>
        </tr>
    </thead>
    <tbody>
        <% if (matriculas && matriculas.length > 0) { %>
            <% matriculas.forEach(mat => { %>
                <tr style="border-bottom: 1px solid #dee2e6;">
                    <td style="padding: 10px;"><strong><%= mat.curso_id.nome %></strong></td>
                    <td style="padding: 10px;"><%= mat.ano_letivo %></td>
                    <td style="padding: 10px;">
                        <span style="padding: 5px 10px; border-radius: 5px; 
                            <%= mat.estado === 'aprovada' ? 'background: #d4edda; color: #155724;' : 
                                mat.estado === 'rejeitada' ? 'background: #f8d7da; color: #721c24;' :
                                'background: #fff3cd; color: #856404;' %>">
                            <%= mat.estado %>
                        </span>
                    </td>
                    <td style="padding: 10px; text-align: center;">
                        <%= new Date(mat.criado_em).toLocaleDateString('pt-PT') %>
                    </td>
                </tr>
            <% }); %>
        <% } else { %>
            <tr>
                <td colspan="4" style="padding: 20px; text-align: center; color: #999;">Nenhuma matrícula encontrada</td>
            </tr>
        <% } %>
    </tbody>
</table>
