# .htaccess (raiz do projeto)
# Bloquear acesso direto a tudo exceto public/
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/academic_system/public/
    RewriteRule ^ - [F,L]
</IfModule>

# Negar acesso à raiz diretamente
Order allow,deny
Deny from all
